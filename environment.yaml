name: SceDiT
channels:
  - pytorch
  - nvidia
dependencies:
  - python == 3.8.5
  - pytorch == 2.4.1
  - torchvision
  - pytorch-cuda=12.1
  - pip:
    - timm
    - matplotlib
    - pandas
    - pyproj
    - scipy
